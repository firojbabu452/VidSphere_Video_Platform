apiVersion: v1
kind: Service
metadata:
  name: vidsphere-frontend-service
  namespace: vidsphere 
spec:
  selector:
    app: vidsphere-frontend

  ports:
    - port: 3000
      targetPort: 3000

  type: ClusterIP