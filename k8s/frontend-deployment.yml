apiVersion: apps/v1
kind: Deployment
metadata:
  name: vidsphere-frontend
  namespace: vidsphere 
  labels:
    app: vidsphere-frontend

spec:
  replicas: 2

  selector:
    matchLabels:
      app: vidsphere-frontend

  template:
    metadata:
      labels:
        app: vidsphere-frontend

    spec:
      containers:
        - name: frontend

          image: firojbabu452/vidsphere-frontend:5be5fc2e74103664b06512c578c83bcbaf974911
          imagePullPolicy: Always

          ports:
            - containerPort: 80

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

            limits:
              cpu: "300m"
              memory: "256Mi"

          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 20
            periodSeconds: 10

          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5